alpaca-py>=0.33.0
yfinance>=0.2.50
duckdb>=1.1.0
pandas>=2.2.0
numpy>=1.26.0
PyYAML>=6.0.0
fastapi>=0.115.0
uvicorn>=0.30.0
# Capped below 2.0: that release renamed FastMCP to MCPServer and dropped
# mcp.server.fastmcp entirely, so an unpinned build breaks --mcp mode at import.
mcp>=1.0.0,<2.0.0
