alpaca-py>=0.33.0
yfinance>=0.2.50
duckdb>=1.1.0
pandas>=2.2.0
numpy>=1.26.0
PyYAML>=6.0.0
fastapi>=0.115.0
uvicorn>=0.30.0
# 2.0 renamed FastMCP to MCPServer and dropped mcp.server.fastmcp; src/mcp_server.py
# supports either, but stay below 2.0 until the import path settles.
mcp>=1.0.0,<2.0.0
# Schwab streamer websocket (real-time quotes, books, trade prints).
websockets>=12.0
