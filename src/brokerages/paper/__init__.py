"""A local paper book: a real Brokerage backed by state store rows rather than by a venue.

No ``client.py`` -- there is nothing to talk to. That is the whole point: an algorithm can be
run, sized and "traded" before any real broker is configured.
"""
