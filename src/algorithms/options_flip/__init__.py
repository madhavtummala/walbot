"""Directional options pick: the macro trend selects call or put, the intraday setup the name.

``config.py`` holds the knobs, ``algorithm.py`` the two gates and the contract they choose,
``signals.py`` the rows the deck renders. Import from the module that owns the name.
"""
